name: Build Snap Package

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
        default: '1.0.0'

jobs:
  build-snap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            echo "version=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          fi

      - name: Build JAR
        run: mvn -B package -DskipTests

      - name: Build Snap
        uses: diddlesnaps/snapcraft-multiarch-action@v1
        with:
          snapcraft-token: ${{ secrets.SNAPCRAFT_TOKEN }}
          snap-file: sudoku_${{ steps.version.outputs.version }}_amd64.snap

      - name: Upload Snap Artifact
        uses: actions/upload-artifact@v4
        with:
          name: sudoku-best
          path: "*.snap"

#      - name: Publish to Snap Store (Canonical Snapcraft)
#        if: startsWith(github.ref, 'refs/tags/')
#        env:
#          SNAPCRAFT_TOKEN: ${{ secrets.SNAPCRAFT_TOKEN }}
#        run: |
#          echo "$SNAPCRAFT_TOKEN" > snapcraft.token
#          snapcraft login --with snapcraft.token
#          snapcraft upload sudoku_*.snap --release stable